{
  "name": "agent-action-firewall",
  "version": "0.1.0",
  "description": "Sequence-aware runtime safety gate for AI agent tool actions.",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "tsx src/demo.ts",
    "start": "tsx src/server.ts",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "^2.0.0-beta.5",
    "@modelcontextprotocol/server": "^2.0.0-beta.5",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0-beta.5",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
